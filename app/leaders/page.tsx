"use client";import {useEffect,useState} from "react";import {supabase} from "../../lib/supabase";import SeasonSelect from "../../components/SeasonSelect";export default function Leaders(){const[season,setSeason]=useState("2025/26"),[rows,setRows]=useState<any[]>([]);useEffect(()=>{(async()=>{const{data}=await supabase.from("player_season_stats").select("*").eq("season",season).order("goal_contributions",{ascending:false}).order("goals",{ascending:false});setRows(data||[])})()},[season]);return <><div className="hero"><div><h1>Player leaders</h1><div className="muted">Starts, goals and assists.</div></div><SeasonSelect season={season} onChange={setSeason}/></div><div className="tablewrap"><table><thead><tr><th>#</th><th>Player</th><th>Team</th><th>Apps</th><th>Starts</th><th>Goals</th><th>Assists</th><th>G+A</th></tr></thead><tbody>{rows.map((r,i)=><tr key={`${r.player_name}-${r.team_name}`}><td>{i+1}</td><td><a href={`/players/${encodeURIComponent(r.player_name)}?season=${encodeURIComponent(season)}`}><b>{r.player_name}</b></a></td><td><a href={`/teams/${encodeURIComponent(r.team_name)}?season=${encodeURIComponent(season)}`}>{r.team_name}</a></td><td>{r.appearances}</td><td>{r.starts}</td><td>{r.goals}</td><td>{r.assists}</td><td><b>{r.goal_contributions}</b></td></tr>)}</tbody></table></div></>}